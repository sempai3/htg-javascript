/*
let favorito = document.querySelector('.comment')
favorito.classList.add('comentFavorito');
//creamos una variable donde se guarda el elemente que seleccionamos querySelector('')

let elementoHr = document.querySelectorAll("hr");
for (let i = 0; i < elementoHr.length; i++){
    elementoHr[i].classList.add('comentFavorito');
}

const hideLargo = document.querySelectorAll('.comment--text');
for (let i=0; i < hideLargo.length; i++){
    if (hideLargo[i].innerText.length > 50){
        hideLargo[i].classList.add('ocultar');
    }   
}
*/
 let comMasLargo = document.querySelectorAll('comment--text')
 
 for (let i=o; i<comMasLargo; i++)